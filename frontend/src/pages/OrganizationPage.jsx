const Organization = () => {
  return (
    <div>
      <h1>Find an Organization Near You</h1>
      {/* TODO: Fetch and display organizations using GET /orgs and GET /orgs/:id */}
    </div>
  );
};

export default Organization;